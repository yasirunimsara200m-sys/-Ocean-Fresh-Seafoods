# In local development, leave this empty so Vite proxy (/api -> localhost:5000) is used.
# When deploying Frontend to Vercel, set this in Vercel Project Settings > Environment Variables:
# VITE_API_URL=https://ocean-fresh-backend.onrender.com
VITE_API_URL=
